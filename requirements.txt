# Dependencies for the deploy/catalog tooling (scripts/deploy.py).
# PyYAML is used to parse SKILL.md frontmatter correctly, including
# multi-line block scalars (description: |) and quoted values.
requests>=2.31
pyyaml>=6.0
anthropic
